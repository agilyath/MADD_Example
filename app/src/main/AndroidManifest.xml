<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vehiclebath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adminSelectUpdateSubscription"/>
        <activity android:name=".selectSubscription"/>
        <activity android:name=".adminAdvertiseDetails"/>
        <activity android:name=".orgYourAdvertisement"/>
        <activity android:name=".orgDeleteAdvertisement"/>
        <activity android:name=".orgUpdateAdvertisement"/>
        <activity android:name=".orgAddAdvertisement"/>
        <activity android:name=".adminUpdateSubscrip"/>
        <activity android:name=".addSubscrip"/>
        <activity android:name=".adminSubscripDetail">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".updateSubscrip"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>